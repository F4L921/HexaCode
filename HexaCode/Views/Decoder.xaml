<Page
    x:Class="HexaCode.Views.Decoder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HexaCode.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="32,20,32,20" Orientation="Vertical">
            <TextBlock Text="Décrypteur" Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Style="{StaticResource DefaultTextBoxStyle}" x:Name="inp" PlaceholderText="Entrez le message à décoder" Width="570" Margin="0, 12" HorizontalAlignment="Left"/>
                <Button Style="{StaticResource AccentButtonStyle}" x:Name="decrypt" Content="Décrypter le message" Margin="12,0,0,0" Click="decrypt_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>